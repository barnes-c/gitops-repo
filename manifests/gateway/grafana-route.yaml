apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: monitoring
spec:
  parentRefs:
    - name: main
      namespace: gateway
  hostnames:
    - grafana.barnes.biz
  rules:
    - backendRefs:
        - name: monitoring-grafana
          port: 80
